server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/SpectraPhantom/config-repo
          search-paths: userservice,itemservice
        encrypt:
          enabled: false